document.addEventListener("DOMContentLoaded", () => {
  const typed = new Typed(".auto-type", {
    strings: ["EventSphere"],
    typeSpeed: 100,
    backSpeed: 50,
    loop: true,
    backDelay: 5000,
    cursorChar: "|",
    smartBackspace: true,
  });
});
